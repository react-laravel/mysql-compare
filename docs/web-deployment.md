# Web deployment

Web (Express + Basic/SSO) deployment is **not maintained on `main`**.

Use the **`electron`** branch for:

- Electron desktop
- Browser / Express web deployment (`docs/web-deployment.md` content and `npm run web:*` scripts)

The `main` branch is Tauri + full Rust backend only.
